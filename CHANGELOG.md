# Changelog

## 0.9.3 — 2026-09-12

**手感默认值与滑杆量程**：指针与拖动速度默认 1.5×（可调 0.2–5.0），滚动倍率默认 0.3×（可调 0.05–5.0），两个滑块的中点是默认值，往左更慢、往右更快，左右两半各按对数刻度，默认值两侧分辨率一致；范围两端都比原来的 0.3–4.0 更宽。历史存档只按新范围夹紧，不会被丢弃。

**轻点后拖动不再误判**：连续滑动鼠标时，手指偶尔会先轻轻碰一下（不足 8 px、240 ms 内），这一下按规则算作单击；此前紧跟其后的正常滑动会被当成"轻点后按住拖动"，于是想要的指针移动变成点按＋拖动。现在"轻点后按住拖动"要求第二次按下先静置 150 ms 再滑动，起手就走的手势仍按指针移动处理。真机复现脚本显示：修前"轻微触碰 → 滑动"得到 click+drag，修后是 click+move，而"轻点 → 按住 → 拖动"照常拖动。

**静态资源按目录提供**：Mac 端网页资源原来写死六个文件名，新增模块会被回 404，页面停在"正在连接 Mac…"。现在按构建产物目录提供 .html／.css／.js，并在缺少 index.html、style.css、app.js 时直接报错；新增回环测试逐个请求 `Web/dist` 中的资源，防止再次出现同类问题。

## 0.9.2 — 2026-09-12

**侧边滚动更可靠**：可滚动范围从最外侧 28 px 窄条扩到与渐隐提示等宽的 76 px 判定带。窄条仍是落指即滚动；判定带内先看方向——首个采样方向明确（纵向或横向达到 2.5 倍）就地判定，否则再等几像素，14 px 内纵向占多数即滚动，横向占多数则回到指针移动。此前拇指落在窄条外几像素、或起手前几像素横向抖动时都会悄悄变成移动指针，看起来就是"侧边滚动时好时坏"；现在这些情况都会正常滚动，滚动时对应一侧的渐隐标记同时加深以作反馈。轻点、双击按住拖动、横滑与中央起手的行为不变；窄条与判定带宽度改由 CSS 变量 `--edge-strip`／`--edge-band` 统一给手势代码读取，不会再出现视觉范围与可触范围不一致。

## 0.9.1 — 2026-09-12

打包收尾：App 内不再夹带已移除依赖的残留资源。`swift-nio-ssl` 在 0.8.0 已从依赖中删除，但构建目录里遗留的 `swift-nio-ssl_NIOSSL.bundle` 会被原样拷进 App；`scripts/build.sh` 现在只拷当前依赖（`swift-nio`／`swift-atomics`／`swift-collections`／`swift-system`）的资源包，遇到其他资源包会跳过并提示，遗留目录也已清除。功能与协议与 0.9.0 相同。

## 0.9.0 — 2026-09-12

允许任意多台设备同时控制：不再有“第二台被 busy 拦下”，每个连接获得独立会话与序号，输入在同一个锁与执行器内串行执行；同一时刻以先开始的手势为准，其他会话的移动／点击／滚动会被忽略直到该手势结束，只有持有指针的那一方断开时才释放按住的按键。Mac 菜单显示控制者数量并可一次“断开全部控制者”；网页端删除 busy 状态。

## 0.8.2 — 2026-09-12

修复开机自启动时服务不启动：登录启动的 App 常常早于网络就绪，那一刻还没有私有 IPv4，服务因此没有开启，而之后也没有任何重试。现在地址一出现就自动开启服务，地址或主机名变化后按新地址自动重启，只有用户主动“停止服务”才保持关闭；菜单在等待网络时显示“正在等待网络…”。规则收敛到 `SofaPadCore.ServiceLaunchPolicy` 并由单元测试覆盖。

## 0.8.1 — 2026-09-12

修复安装包落点：`pkgbuild` 会把 App 标记为可重定位（`<relocate>`），macOS 安装器因此会全盘查找同标识的 SofaPad.app 并**就地更新那一份**——只要仓库里存在 `build/SofaPad.app`，双击 pkg 就不会装进 `/Applications`，而是覆盖该构建产物。打包脚本现在展开并移除该元素后重新封装，安装位置只由 `--install-location` 与 payload 决定（使用 `PKG_SIGNING_IDENTITY` 时改为先封装再 `productsign`）。App 代码与 0.8.0 相同。

## 0.8.0 — 2026-09-12

**简化连接方式**：去掉配对、二维码、Cookie 与 HTTPS，改为“打开 App 即自动开启服务、同网段任意设备用浏览器打开菜单栏显示的地址即可控制”。服务只在绑定的私有 IPv4 上提供明文 HTTP/WS；保留 Host／Origin 精确校验、同源要求、单控制者与协议限额。工程随之删掉 swift-nio-ssl 依赖、凭据存储、证书导入与相关测试，Mac 侧不再有设置窗口或配对窗口，全部设置都在菜单栏菜单里（状态、地址、开启／停止、断开、权限、登录启动、退出）。新增正式 App 图标（`MacApp/AppIcon.icns`，由 `scripts/make-icon.sh` 生成并随构建打包）。

**输入体验**：输入模式改为覆盖在触控板上的顶部下拉面板，输入框固定一行，已提交字符先在输入光标处以略大字号停留约 0.5 秒再向上飘出淡出；删除显示 ⌫、回车在 v2 中新增固定 enter（真正的 kVK_Return 按键）；每次编辑发送一条 edit（退格 + 合成文本），不经过剪贴板；IME 候选不出手机，拼音纠错与组合收尾的退格不会误删 Mac 内容；语音听写“停顿即发送”（停顿约 1.2 秒或结束听写时才发送，识别修正只影响未发送部分）；点击面板以外或收起键盘回到鼠标模式。

**工程**：`scripts/package.sh` 生成 `.pkg`／`.zip`（可选 `.dmg`），`scripts/make-icon.sh` 重新生成图标；构建产物不再进入 Spotlight（`build/.metadata_never_index`）；补充本地安装、更新、历史版本清理与卸载说明；`scripts/test.sh` 自动补齐网页依赖并支持 SwiftPM 参数透传。验证记录同步为 82/82 全绿（网页 52、Swift 19、HTTP/WS 回环 11）。

## 0.7.0 — 2026-09-11

移除单手遥控模式、速度引擎和相关设置，回归整屏触控板／输入两模式。触控板恢复静止 500 ms 右击，保留双指右击、双指滚动、三指与双击按住拖动。左右各 28 px 渐隐窄区支持纵向滚动，落指锁定区域、混区加指取消；沿用 0.4 滚动基础系数。右上角精简为鼠标／键盘双图标胶囊，保留当前状态高亮；升级保留触控板手感并清理旧遥控配置。产品基线 v2.5，协议仍为 v2。

## 0.6.0 — 2026-09-10

默认遥控，设置中遥控排在前面，继续保存用户选择。移除触控板长按右击，遥控静止长按改为 2 秒。屏蔽控制面板与控件的原生选中／长按菜单，保留文本框的原生选字。遥控使用 4 px 死区、渐进速度曲线和独立速度倍率，移动按计算当帧发送，回中／抬手不补发尾部位移；左右各 28 px 滚动区以渐隐边缘融入面板。产品基线更新 v2.4，协议仍为 v2。

## 0.5.0 — 2026-09-10

触控板与遥控滚动基础速度统一降至原来的 40%，保留已有倍率与自然滚动方向。右上角改为控制／键盘双图标与双向箭头的胶囊切换按钮，高亮当前模式。输入模式左上角新增退格图标，每次点击向 Mac 发送一次固定 Backspace 按下／抬起，保留手机草稿和键盘焦点。协议 v2 增加能力声明与 backspace 类型，受既有鉴权、权限、序号和活动手势限制；不排队、不自动重发。产品基线更新 v2.3。

## 0.4.0 — 2026-09-10

实现单手遥控：以落指点为中心的方向与速度控制，10 px 防抖死区、线性变速与上限、回中／抬手停止；界面仅使用方向渐变，无摇杆图形。支持轻点／双击、静止 3 秒右击、轻点后再次按住偏移持续拖动，以及最右侧 28 px 纵向滚动区。两种模式共用手感设置和输入；取消、加指、切模式及断线清理持续帧和按下状态。协议仍为 v2，统一产品基线更新为 v2.2。

## 0.3.0 — 2026-09-10

触控板新增单指长按右击、轻点后第二次按住滑动拖动，保留原单／双／三指手势。左上角设置增加触控板／遥控选择；遥控只提供独立占位视图、选择保存和共用输入，实际单手操作待定义。模式切换取消长按和拖动，沿用协议 v2。同步产品基线 v2.1 与回归测试。

## 0.2.0 — 2026-09-09

合并 MVP 产品稿与旧蓝图，统一产品基线 v2.0，保留原稿归档。手机改为整屏触控／输入两模式；按最新补充保留左上角手感设置。新增三指拖动、完整文本粘贴、IME 提交处理、草稿保留、去重与结果不确定反馈。

协议升级 v2，Mac 增加拖动释放与剪贴板成功后 Command + V 的可测试执行后端。内嵌 HTTPS/WSS 并支持导入可信 PEM 材料，未加密 HTTP 限显式开发模式。简化 Mac 设置；补充单元、真实 HTTP/WSS 与桌面浏览器回归。iPhone Safari 三指、原生键盘、真实粘贴和电视体验仍待验收。

## 0.1.0 — 2026-09-09

首个开发预览：Swift 原生菜单栏与设置、内置 TypeScript 触控页、HTTP/WS 配对/会话、CGEvent 执行器、双指手势、重连与手感设置、权限和登录启动指引。提供锁定依赖、App 打包、假执行器、共享协议样例及自动测试。

默认关闭、用户主动接受的家庭 HTTP 模式；首选选定私有 IPv4，主机名可选。尚未完成 iPhone Safari、真实鼠标事件、电视体验或公证分发验收。
